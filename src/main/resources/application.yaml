spring:
  application:
    name: gestion-absences

  datasource:
    url: jdbc:mysql://localhost:4306/gestion_absences
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
